---
import { getRelativeLocaleUrl } from "astro:i18n";

import NavItem from "./NavItem.astro";

interface Props {
  navItems: {
    title: string;
    href: string;
  }[];
  // navMinHeight: number;
  lang: string;
}

const { navItems, lang } = Astro.props;
---

<nav
  class="hide-me z-100 pointer-events-auto relative hidden w-screen items-center justify-center gap-3 md:flex"
>
  {
    navItems.map((item) => {
      const { href, title } = item;
      const link = getRelativeLocaleUrl(lang, href);
      return <NavItem link={link} title={title} />;
    })
  }
</nav>
