---
import ImageWrapper from "./ImageWrapper.astro";

interface Props {
  title: string;
  body: string;
  icon: {
    iconLogo: {
      image: string;
      alt: string;
    };
    iconColor: string;
  };
}

const { title, body, icon } = Astro.props;
---

<div>
  <div>
    <ImageWrapper src={icon.iconLogo.image} altText="Services Logo" />
  </div>
  <h2>{title}</h2>
  <p>{body}</p>
</div>
