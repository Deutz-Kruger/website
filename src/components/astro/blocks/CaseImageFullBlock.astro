<!-- ---
import ImageWrapper from "@components/astro/util/ImageWrapper.astro";
import type { z } from "astro:content";

import type { caseImageFullBlock } from "@/content/config";
import { cn } from "@/utils/cn";

import Carousel from "../Carousel.astro";
import Background from "./utils/Background.astro";

type CaseImageFullBlock = z.infer<typeof caseImageFullBlock>;

interface Props {
  block: CaseImageFullBlock;
  loading: string;
  className?: string;
  index: number;
}
// eslint-disable-next-line @typescript-eslint/no-unused-vars
const { block, loading, className = "", index } = Astro.props;
const { images, background } = block;
---

<div class={cn(className, "md:col-span-full")}>
  <Background background={background} className="">
    <div class={cn("md:hidden")}>
      <Carousel>
        {
          images.map((image) => (
            <div class="swiper-slide !w-2/3 p-4">
              <ImageWrapper
                src={image.image}
                altText={image.alt}
                loading={loading}
                className="w-full md:w-fit rounded-media"
              />
            </div>
          ))
        }
      </Carousel>
    </div>

    <div class={cn("hidden md:grid md:grid-cols-2 lg:grid-cols-4 gap-4")}>
      {
        images.map((image) => (
          <div class="py-4">
            <ImageWrapper
              src={image.image}
              altText={image.alt}
              loading={loading}
              className="mx-auto md:w-fit object-contain rounded-media"
            />
          </div>
        ))
      }
    </div>
  </Background>
</div> -->
